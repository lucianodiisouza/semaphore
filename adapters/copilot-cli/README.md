# Copilot CLI adapter

Config: `~/.copilot/hooks.json` (when available in your Copilot CLI version)

| Event | Light |
|-------|-------|
| `UserPromptSubmit` | yellow |
| `PreToolUse` | red |
| `PostToolUse` | yellow |
| `Stop` | green |

**Note:** Copilot CLI hook surface varies by version. Run `semctl doctor` after install.
